services:
  app:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - qam-data:/app/data
    restart: unless-stopped

volumes:
  qam-data:
